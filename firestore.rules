rules_version = '2';
// Only authenticated (logged-in) users can read/write ProLift data.
// This replaces a traditional backend's access control.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
